## NETFLIX (HOW DID I APPROACH)
  
# Routes 
   
 - **Body**
     - route was define in App.js with "Body" beign the default path and header defined in it.
     - with its children in it by using "Outlet" 
     {LandingPage, SignIn}
- **Browse**
    - browse is the second route with header define in it too.
    - and have children as the path chages in browser
    {MainContainer, SecondaryContainer,SearchGpt}


# App Returns
  - it has Provider(Redux) for store 
  - RouterProvider in it 
